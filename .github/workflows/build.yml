name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # Обязательно ставим JDK, чтобы дальше не было проблем
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Create Web App Manifest (если нет)
        run: |
          if [ ! -f "manifest.json" ]; then
            echo '{"name": "My App", "short_name": "App", "start_url": "https://example.com", "display": "standalone", "background_color": "#ffffff", "theme_color": "#000000", "icons": [{"src": "icon.png", "sizes": "512x512", "type": "image/png"}]}' > manifest.json
          fi

      # ВАЖНО: Мы полностью обходим 'init', который задаёт вопросы.
      # Мы вручную создаём структуру проекта без интерактива.
      - name: Prepare TWA project manually
        run: |
          mkdir -p twa-manifest
          npx @bubblewrap/cli generate --manifest manifest.json --output twa-manifest
          cd twa-manifest
          npx @bubblewrap/cli validate

      # Сборка происходит внутри папки
      - name: Build APK
        working-directory: ./twa-manifest
        run: npx @bubblewrap/cli build --yes

      # Загружаем результат
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: twa-manifest/app-release.apk
