name: Build APK (Абсолютная и окончательная победа)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # Манифест с безопасной встроенной SVG иконкой (без ошибок Invalid URL)
      - name: Create Web App Manifest
        run: |
          cat > manifest.json <<EOF
          {
            "name": "My App",
            "short_name": "App",
            "start_url": "https://www.google.com",
            "display": "standalone",
            "background_color": "#ffffff",
            "theme_color": "#000000",
            "icons": [{
              "src": "data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 512 512'%3E%3Crect width='512' height='512' fill='%23333'/%3E%3Ctext x='50%25' y='50%25' font-size='100' text-anchor='middle' dy='.3em' fill='white'%3EApp%3C/text%3E%3C/svg%3E",
              "sizes": "512x512",
              "type": "image/svg+xml"
            }]
          }
          EOF

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Install expect
        run: sudo apt-get install -y --no-install-recommends expect

      # ПРАВИЛЬНЫЙ expect: Отвечаем на ВСЕ вопросы, даже про пути к JDK/SDK!
      - name: Generate TWA Project
        run: |
          expect <<EOF
          set timeout 60
          spawn npx @bubblewrap/cli generate --manifest manifest.json --output android
          expect {
              "Do you want Bubblewrap to install the JDK" { send "n\r"; exp_continue }
              "Path to your existing JDK" { send "$::env(JAVA_HOME)\r"; exp_continue }
              "Do you want Bubblewrap to install the Android SDK" { send "n\r"; exp_continue }
              "Path to your existing Android SDK" { send "$::env(ANDROID_HOME)\r"; exp_continue }
              "Do you agree to the Android SDK terms" { send "y\r"; exp_continue }
              eof { exit 0 }
          }
          EOF

      # СБОРКА: Заходим в созданную папку android и компилируем
      - name: Build APK using Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew :app:assembleDebug

      # Итоговый путь к файлу
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/debug/app-debug.apk
